# CLI-Based 3D Object Viewer

This project is a simple Rust-based viewer in the terminal that displays a
three-dimensional object from a local filepath.

## Usage

Since this is a Rust-based project, you can build and run it with:

```zsh
cargo build
cargo run -- help  # Lists available commands and usage
```
